SECRET_KEY=replace-with-a-long-random-secret
DB_USER=student_app
DB_PASSWORD=your-database-password
DB_HOST=localhost
DB_NAME=student_moneye
# Use true only when the deployed app is served over HTTPS.
COOKIE_SECURE=false
